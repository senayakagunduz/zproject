import React from 'react'
import {Swiper,SwiperSlide} from 'swiper/react'
import slides from "./slides.json"

const Slider = () => {
  return (
    <Swiper>
        {/* {slides.map(slide=><SwiperSlide key={slide.id}>hello</SwiperSlide>)} */}
        
    </Swiper>
  )
}

export default Slider